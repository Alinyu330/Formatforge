# FormatForge 使用说明

> ⚠️ **声明**：本工具仅供个人学习与使用，**禁止任何商业盈利性行为**。请勿将本工具用于商业用途、付费服务、营利性分发或任何形式的牟利活动，否则后果自负。

---

## 一、访问域名

| 用途 | 地址 |
|------|------|
| **主站（推荐）** | https://formatforge.asia |
| **备用站（GitHub Pages）** | https://alinyu330.github.io/Formatforge |
| 备用别名 | https://www.formatforge.asia（自动跳转到主站） |

> 说明：
> - 主站与备用站内容一致，任一域名打不开时可切换另一个访问。
> - 首次访问建议稍等片刻（加载转换引擎），之后可刷新或安装为应用。
> - `qq.formatforge.asia` 为 QQ 音乐解密的内部代理域名，由程序自动调用，无需手动访问。

---

## 二、不同设备的使用方法

本工具支持 **网页端（PWA）**、**手机端**、**电脑桌面客户端** 三种使用方式，转换能力一致。

### 1. PC 网页端（Windows / macOS / Linux）

1. 用 **Chrome** 或 **Edge** 浏览器打开主站 https://formatforge.asia
2. 点击地址栏右侧的「安装」图标，即可安装为桌面应用
3. 安装后从桌面/开始菜单直接启动，首次加载完转换引擎后可完全离线使用

### 2. 手机端（Android / iOS）

- **Android**：
  1. 可**直接下载安装 Android 客户端（APK）**：https://formatforge.asia/Formatforge/FormatForge-v16.1.apk （手机直接点击下载；下载后点击安装，需允许「安装未知来源应用」；若已装旧版请先卸载再安装）
     - 若上述国内域名无法访问，备用下载：https://github.com/Alinyu330/Formatforge/releases/download/backup-20260820-v16/FormatForge-v16.apk
  2. 也可用 Chrome 打开主站 → 菜单「添加到主屏幕」/「安装应用」，即可像 App 一样使用
  3. 客户端转换引擎已打包在应用内，加载更快
- **iOS（iPhone / iPad）**：
  1. 用 Safari 打开主站 → 底部「分享」→「添加到主屏幕」
  2. 命名后添加，主屏幕即出现应用图标

### 3. 电脑桌面客户端（Electron）

- 使用原生 FFmpeg（ffmpeg.exe）进行转换，性能与转换速度最佳
- 解密酷狗 KGG 时会优先自动读取本机已登录酷狗客户端的密钥库，无需手动导入

---

## 三、音频转换

### 1. 音频格式互转

支持的源格式：**MP3、FLAC、WAV、AAC、OGG、M4A、WMA、OPUS、APE、WV、AIFF、ALAC、AC3、EAC3、AMR、AU、WEBM** 等

可转换的目标格式：**MP3、FLAC、WAV、AAC、OGG、M4A、WMA、OPUS、WEBM**

操作：选择音频文件 → 选择目标格式 → 可调整比特率/采样率 → 点击「开始转换」

### 2. 加密音频解密

以下平台加密音乐可直接解密为普通音频：

| 平台 | 加密格式 |
|------|---------|
| **QQ 音乐** | QMC0、QMC3、QMC4、QMC6、QMC8、QMCflac、QMCogg、MGG、MGG1、MGG2、MFLAC、MFLAC0、MFLAC2、BKcmp3、BKcflac、TKM、TKM3、TKM4 |
| **网易云音乐** | NCM |
| **酷狗音乐** | KGM、KGMA、KWM、VPR、KGG |

解密后同样可转换为上述任意目标音频格式。

#### 各加密格式的解密要求

- **QQ 音乐（QMC/MGG/MFLAC/TKM 等）**：新版文件需在页面填写 QQ 音乐 UIN 与登录凭证（authst 或 qqmusic_key，或完整 Cookie），程序自动向官方接口获取密钥后解密；旧版文件无需登录即可解密。
- **网易云音乐（NCM）**：直接解密，无需额外配置。
- **酷狗旧版（KGM/KGMA/KWM/VPR）**：直接解密，无需额外配置。
- **酷狗新版（KGG）**：需要酷狗客户端密钥库（KGMusicV3.db）才能解密，详见下文「KGG 解密说明」。

### 3. KGG 解密说明（重点）

KGG 文件（如 `歌曲.kgg.flac`）解密需要**酷狗客户端密钥库**，不同设备操作如下：

- **电脑端（Electron 客户端）**：优先自动读取本机已安装并登录的酷狗客户端密钥库；若未找到，可在酷狗客户端「设置」中导出密钥（复制密钥文本）或直接导入 `KGMusicV3.db` 文件。
- **电脑端（网页）**：在「酷狗 KGG 密钥库」处导入本机酷狗客户端的 `KGMusicV3.db` 文件（路径：`%APPDATA%\KuGou8\KGMusicV3.db`，需已登录酷狗客户端）。
- **手机端（Root 手机）**：程序自动检测到 Root 权限后，可在音频选项中点击「自动读取密钥库」，直接读取手机中已安装并登录的酷狗音乐客户端密钥库，一键解密，无需手动导入。
- **手机端（未 Root）**：程序会提示无法读取本机密钥库。此时需在电脑端酷狗客户端「复制密钥文本」发送到手机，在手机端「手机端粘贴导入密钥」中粘贴导入（纯本地解析，不会上传）。

> 密钥库必须来自**下载该歌曲的同一酷狗账号**，且为最新版本，否则会提示找不到对应密钥。

---

## 四、视频转换

支持的源格式：**MP4、MKV、WEBM、MOV、AVI、FLV、WMV、MPEG、MPG、M4V、3GP、TS、OGV**

可转换的目标格式：**MP4、MKV、WEBM、MOV、AVI、FLV、WMV、MPEG、MPG、M4V、3GP、TS、OGV、GIF**（GIF 为提取动态图）

操作：选择视频文件 → 选择目标格式 → 可选编码质量（高/中/低）、分辨率（原画/1080p/720p/480p）、帧率 → 点击「开始转换」

---

## 五、图片转换

支持的源格式：**PNG、JPG、JPEG、WEBP、BMP、ICO、TIFF、TIF、GIF、SVG**

可转换的目标格式：**PNG、JPEG、WEBP、BMP、ICO、TIFF、PDF**（PDF 为把图片合成 PDF）

操作：选择图片文件 → 选择目标格式 → 点击「开始转换」

---

## 六、表格转换

支持的源格式：**XLSX、XLS、CSV、ODS、HTML**

可转换的目标格式：**XLSX、XLS、XLSB、XLSM、ODS、FODS、CSV、TXT、HTML**

操作：选择表格文件 → 选择目标格式 → 点击「开始转换」

---

## 七、办公文档转换

### 1. PDF

可转换的目标格式：**PPTX、DOCX、XLSX、PNG、JPEG、TXT**

### 2. Word（DOCX）

可转换的目标格式：**DOCX、TXT、HTML、PDF**

### 3. PowerPoint（PPTX）

可转换的目标格式：**PPTX、TXT、HTML**

操作：选择文档文件 → 选择目标格式 → 点击「开始转换」

---

## 八、格式互转速查表

| 类别 | 源格式 | 可转换目标格式 |
|------|--------|---------------|
| 音频 | MP3 / FLAC / WAV / AAC / OGG / M4A / WMA / OPUS / WEBM / APE / WV / AIFF / ALAC / AC3 / EAC3 / AMR / AU | MP3、FLAC、WAV、AAC、OGG、M4A、WMA、OPUS、WEBM |
| 音频 | QQ 音乐（QMC/MGG/MFLAC/TKM 等） | 解密后同上 |
| 音频 | 网易云 NCM | 解密后同上 |
| 音频 | 酷狗 KGM / KGMA / KWM / VPR / KGG | 解密后同上 |
| 视频 | MP4 / MKV / WEBM / MOV / AVI / FLV / WMV / MPEG / MPG / M4V / 3GP / TS / OGV | MP4、MKV、WEBM、MOV、AVI、FLV、WMV、MPEG、MPG、M4V、3GP、TS、OGV、GIF |
| 图片 | PNG / JPG / JPEG / WEBP / BMP / ICO / TIFF / TIF / GIF / SVG | PNG、JPEG、WEBP、BMP、ICO、TIFF、PDF |
| 表格 | XLSX / XLS / CSV / ODS / HTML | XLSX、XLS、XLSB、XLSM、ODS、FODS、CSV、TXT、HTML |
| 文档 | PDF | PPTX、DOCX、XLSX、PNG、JPEG、TXT |
| 文档 | DOCX | DOCX、TXT、HTML、PDF |
| 文档 | PPTX | PPTX、TXT、HTML |

---

## 九、隐私与安全

- 所有转换均在**本地设备**完成（浏览器内 WASM 引擎或桌面端原生 FFmpeg），**文件不会上传到任何服务器**。
- 仅解密类操作（如 QQ 音乐获取密钥）会请求对应官方接口，且仅传递必要的凭证信息用于解密。
- 如遇页面空白或异常，请尝试 **Ctrl+Shift+R 强制刷新** 或清除浏览器站点数据后重试。

---

*本说明文档随 v16 版本整理，后续功能更新请以实际界面为准。*
